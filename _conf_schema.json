{
  "tts_provider_id": {
    "type": "string",
    "description": "TTS Provider ID（如 edge_tts）",
    "hint": "填写 AstrBot 中已配置的 TTS Provider ID",
    "default": "edge_tts"
  },
  "max_text_length": {
    "type": "int",
    "description": "超过此长度的文本不发送语音（字符数）",
    "hint": "避免长消息生成过长的语音",
    "default": 500
  }
}
